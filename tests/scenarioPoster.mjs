import fs from 'node:fs';import assert from 'node:assert/strict';import {scenarioPoster} from '../src/resources/scenarioPoster.js';
let count=0,missing=0,largest='';for(const name of fs.readdirSync('public/curriculums').filter(f=>f.endsWith('.json'))){const d=JSON.parse(fs.readFileSync('public/curriculums/'+name));d.scenarios.forEach((s,i)=>{try{const html=scenarioPoster(s,name.replace('English_Curriculum_','').replace('.json',''),i);assert.ok(html.includes('Grammar lab'));assert.ok(!html.includes('[object Object]'));if(html.length>largest.length)largest=html;count++;}catch(e){if(!e.message.includes('no contiene'))throw e;missing++;}});}
assert.equal(count+missing,112);const injected=scenarioPoster({title:'<script>alert(1)</script>',grammar:['<img src=x onerror=alert(1)>']},'Kinder',0);assert.ok(!injected.includes('<script>'));assert.ok(injected.includes('&lt;img'));
fs.mkdirSync('tmp/poster',{recursive:true});fs.writeFileSync('tmp/poster/largest.html',largest);console.log({count,missing});
